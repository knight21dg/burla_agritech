# Shell scripts and unit files run on Linux. A stray CRLF makes a shell
# script fail with "bad interpreter", which is a puzzling way to spend an
# afternoon, so their line endings are pinned whoever commits them.
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
